# ============================================================
# 反向白名单(whitelist):管理"家目录本身就是 git 仓库"的场景
#
# 思路:第 1 行 /* 先忽略家目录顶层的"一切",后面再逐个 ! 放行要跟踪的。
# 效果:以后任何新目录/文件(Yocto、构建产物、.cache、node_modules…)
#       都自动被忽略,git status 永远只扫下面这几项 —— 不会再变慢。
#
# 关键概念:已经被 git "跟踪"的文件,不受 .gitignore 影响。
#   所以 .config/ 和 memo/ 里"已经跟踪"的文件照常工作、照常显示改动,
#   不需要在下面单独放行;它们目录里"新增的未跟踪文件"仍被忽略(与以前一致)。
#   若要把新文件加进这类已忽略目录,用:  git add -f <路径>
# ============================================================

# 1) 先忽略家目录顶层的一切
/*

# 2) 再放行你要跟踪的顶层文件
!/.gitconfig
!/.gitignore
!/.p10k.zsh
!/.tigrc
!/.tmux.conf
!/.xinitrc
!/.xprofile
!/.zshrc
!/readme

# 3) .config:只监控你在本仓库维护的子目录;
#    nvim/ 和 yazi/ 是各自独立的 git 仓库,必须排除,绝不能被本仓库吞进来。
#    注意:.gitignore 不支持行尾注释,规则必须独占一行。
# 先放行 .config 目录本身
!/.config/
# 再忽略它下面的一切(nvim、yazi、备份、系统垃圾都挡在外面)
/.config/*
# 然后逐个放行要监控的子目录(连新文件也会显示)

/.config/lazygit/*
!/.config/lazygit/config.yml

!/.config/tmux/
!/.config/wezterm/

# 4) memo:下载/编译临时区。只保留已跟踪的两个文件,
#    其余(wget 下来的 .deb、解压出的源码目录等)全部忽略,不污染 git status。
/memo/*
!/memo/caps-to-ctrl.md
!/memo/install.sh
